__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.env
.venv/
venv/
.pytest_cache/
.mypy_cache/
.DS_Store

# Streamlit / logs
.streamlit/secrets.toml
*.log

# Keep bundled raw CSVs and generated cache files trackable for deployment.
# Large one-off local rebuild artefacts can be ignored manually if needed.
subunit_tracking_clean/

# Keep generated cache/output files trackable for deployment.
# Do not ignore data/cache/ or data/outputs/ because the app should open from the real OpenAlex cache.

# Large generated output CSVs exceed GitHub's 100 MB file limit.
# They can be regenerated from the OpenAlex parquet cache.
data/outputs/scraped_publications_ndph.csv
