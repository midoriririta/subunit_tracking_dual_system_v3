__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.env
.venv/
venv/
.pytest_cache/
.mypy_cache/
.DS_Store

# Streamlit / logs
.streamlit/secrets.toml
*.log

# Keep bundled raw CSVs and generated cache files trackable for deployment.
# Large one-off local rebuild artefacts can be ignored manually if needed.
subunit_tracking_clean/

# Bundled deployment data
# Keep initial raw staff CSVs, generated cache files, and generated output CSVs
# in the repo so Streamlit can open immediately without a first-run scrape.
